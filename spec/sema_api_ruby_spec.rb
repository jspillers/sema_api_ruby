require 'spec_helper'

describe SemaApiRuby do
  it 'has a version number' do
    expect(SemaApiRuby::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
